<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.comoyo.mapper.UserMapper">
    <insert id="register">
        insert into user
            (username, password, name, avatarURL, bio, location, email, github, bilibili, role, created_at, updated_at)
        values
            (#{username}, #{password}, #{name}, #{avatarURL}, #{bio}, #{location}, #{email}, #{github}, #{bilibili},
             #{role}, #{createdAt}, #{updatedAt})

    </insert>
    <delete id="deleteById">
        delete from user
        where id = #{id};
    </delete>

    <select id="gerUserById" resultType="com.comoyo.pojo.User">
        select u.*
        from user u
        where id = #{id};
    </select>
</mapper>